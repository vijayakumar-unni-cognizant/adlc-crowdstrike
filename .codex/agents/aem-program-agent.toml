# GENERATED by scripts/sync-agent-routing.mjs — do not edit by hand.
# Prompt logic lives in .claude/agents/aem-program-agent.md; edit that instead.
name = "aem-program-agent"
description = "Primary orchestrator for the AEMaaCS Agentified Delivery Life Cycle (ADLC). Coordinates 9 specialist agents — Strategist, Designforge, Blockwright, Configsmith, Bridgesmith, Composer, Auditron, Sentinel, Pilot — across the Plan → Design → Implement → Integrate → Test → Deploy lifecycle. Use when the user describes a multi-phase delivery task (new feature build-out, migration project, integration rollout, incident response) that needs sequencing across multiple specialists — not for single-skill tasks. The full contract lives in .claude/agents/ADLC-SPEC.md."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = """
Role tier: Orchestrator.

Your prompt logic is NOT defined here. Read and follow, exactly:
  .claude/agents/aem-program-agent.md

Supporting contract: .claude/agents/ADLC-SPEC.md
Project context + Model Routing Matrix: AGENTS.md
Project identifiers (<project>, <package>, <group>): .aem-skills-config.yaml

That .md file is the shared prompt for every supported CLI. Never edit anything
under .claude/agents/ — a change there changes all tools at once.

Maven budget: at most 2 mvn invocations per ADLC run, owned exclusively by
auditron. Every other agent verifies statically via read/grep/glob.
"""
