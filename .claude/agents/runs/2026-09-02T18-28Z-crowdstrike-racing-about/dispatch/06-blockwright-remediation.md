agent: blockwright
stage: Implement remediation
input-packet: |
  Re-dispatch for run `2026-09-02T18-28Z-crowdstrike-racing-about`. The Implement gate failed on objective static validation. Read `.claude/agents/blockwright.md`, your original packet `dispatch/03-blockwright.md`, Designforge's complete pack, and the recorded decision in `DECISIONS.md`. Do not run Maven. You are not alone in the worktree: preserve Configsmith/Composer edits and modify only your ownership surface.

Required fixes before replacing `handoffs/blockwright.yaml`:
  1. Create the complete new racing-page editable-template subtree at the project-correct FileVault location described by `design/template-design.md` and your contract. It is currently absent from both `ui.content` and `ui.apps`. Include correct structure, initial content, policy MAPPING, no structural Title, project Container proxy, canonical locked Experience Fragment header/footer references, every inner editable-container mapping, page-level `cq:policy`, exact allowlists, and allowed-template registrations. Do not own or rewrite Configsmith's consolidated policy definition file.
  2. Finish the UI test migration. `ui.tests/test-module/package.json` declares Playwright but `package-lock.json` still declares `cypress-eaas`, Cypress dependencies/plugins, and old package metadata. Regenerate it so `npm ci` is reproducible, remove Cypress configurations/directories/dependencies/README references, and ensure the applicable Docker image/harness configuration is Playwright-compatible. Do not alter framework-agnostic Maven/assembly files.
  3. Retry static install/discovery/lint with the contract-required non-Maven commands. If restricted networking blocks dependency retrieval, surface the actual failed command/log and request needed execution authority; do not mark discovery successful without evidence. Once dependencies are available, `npx playwright test --list` must exit 0 and discover all 20 tagged UI specs.
  4. Replace the handoff with contract-complete fields: status `pass` only on valid gates; `ui_tests.harness_state_on_entry`, `cypress_fully_removed: true`, `package_lock_regenerated: true`, `pom_and_assembly_unmodified`, full `specs_authored`, `scenario_coverage: {scenarios_total: 20, scenarios_with_spec: 20, unmapped: []}`, discovery command/exit/counts, eslint result, dual-tier parameterization, and `executed: false`. Include the template path and policy pointers.

On completion, update `implement/blockwright/ui-test-harness.md`, `handoff-notes.md`, and the expected Blockwright handoff. This re-dispatch is iteration 2 of 3 for the Build stage.
expected-handoff: C:\AEM\Repos\adlc-crowdstrike\.claude\agents\runs\2026-09-02T18-28Z-crowdstrike-racing-about\handoffs\blockwright.yaml
gate-criteria: |
  Racing-page template/mappings and allowed-template registration exist; all mappings resolve to Configsmith-provisioned policy paths; Cypress is absent from source/config/dependency lock and documentation; package-lock and package JSON agree; Playwright discovery/lint evidence is successful; all 20 scenario IDs map to specs; no Maven ran; and the handoff uses every mandatory UI-test provenance field.
