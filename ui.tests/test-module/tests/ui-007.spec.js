const { test, expect } = require('@playwright/test'); const { open, oneH1, noSeriousAxe } = require('./helpers'); test('UI-007 landmarks and skip link', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await open(page); await expect(page.locator('.cmp-site-header__skip-link')).toHaveAttribute('href', '#main-content'); await oneH1(page); await noSeriousAxe(page); });
