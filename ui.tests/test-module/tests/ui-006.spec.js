const { test, expect } = require('@playwright/test'); const { open } = require('./helpers'); test('UI-006 mobile menu state', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await page.setViewportSize({ width: 390, height: 844 }); await open(page); const menu = page.locator('.cmp-site-header__menu-toggle'); await menu.click(); await expect(menu).toHaveAttribute('aria-expanded', 'true'); await page.keyboard.press('Escape'); await expect(menu).toHaveAttribute('aria-expanded', 'false'); });
