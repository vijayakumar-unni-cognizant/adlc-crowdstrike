const { test, expect } = require('@playwright/test'); const { open, oneH1 } = require('./helpers'); test('UI-020 canonical page census', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await open(page); await oneH1(page); await expect(page.locator('.cmp-teaser--racing-hero')).toHaveCount(1); await expect(page.locator('.cmp-teaser--racing-editorial')).toHaveCount(4); });
