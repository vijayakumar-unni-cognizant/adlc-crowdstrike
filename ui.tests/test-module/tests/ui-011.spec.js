const { test, expect } = require('@playwright/test'); const { open } = require('./helpers'); test('UI-011 stacked DOM reading order', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await page.setViewportSize({ width: 390, height: 844 }); await open(page); await expect(page.locator('.cmp-teaser--racing-editorial .cmp-teaser__content').first()).toBeVisible(); });
