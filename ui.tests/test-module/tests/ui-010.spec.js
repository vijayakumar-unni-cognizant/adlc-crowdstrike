const { test, expect } = require('@playwright/test'); const { open } = require('./helpers'); test('UI-010 desktop fifty-fifty panes', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await open(page); const values = await page.locator('.cmp-teaser--racing-editorial .cmp-teaser__content').evaluateAll((els) => els.map((el) => getComputedStyle(el).flex)); expect(values.every((value) => value === '0 0 50%')).toBeTruthy(); });
