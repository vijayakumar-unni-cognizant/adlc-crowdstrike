const { test, expect } = require('@playwright/test'); const { open } = require('./helpers'); test('UI-012 editorial CTA and list semantics', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await open(page); await expect(page.locator('.cmp-teaser--racing-editorial').first().getByRole('link')).toHaveCount(1); await expect(page.locator('.cmp-teaser--racing-editorial').last().locator('ul > li')).toHaveCount(3); });
