const { test, expect } = require('@playwright/test'); const { open } = require('./helpers'); test('UI-008 hero mapping and image', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); await open(page); await expect(page.locator('.cmp-teaser--racing-hero h1')).toHaveText(/About Us/i); await expect(page.locator('.cmp-teaser--racing-hero img')).toHaveAttribute('src', /content\/dam/); });
