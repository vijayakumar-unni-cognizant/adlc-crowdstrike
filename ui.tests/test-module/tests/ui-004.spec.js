const { test, expect } = require('@playwright/test'); const { open } = require('./helpers'); test('UI-004 fluid widths do not overflow', async ({ page }, info) => { test.skip(info.project.name === 'author-chromium'); for (const width of [320,360,390,768,1024,1199,1200,1440,1920]) { await page.setViewportSize({ width, height: 900 }); await open(page); expect(await page.evaluate(() => document.documentElement.scrollWidth <= document.documentElement.clientWidth)).toBeTruthy(); } });
