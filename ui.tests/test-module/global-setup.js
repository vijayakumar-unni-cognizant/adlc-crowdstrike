const { request } = require('@playwright/test'); const fs = require('fs'); const path = require('path'); const state = path.join(__dirname, '.auth', 'state.json');
module.exports = async () => { if (process.env.PLAYWRIGHT_SKIP_AUTHOR_LOGIN === 'true') return; const baseURL = process.env.AEM_AUTHOR_URL || 'http://localhost:4502'; fs.mkdirSync(path.dirname(state), { recursive: true }); const ctx = await request.newContext({ baseURL, ignoreHTTPSErrors: true }); const response = await ctx.post('/libs/granite/core/content/login.html/j_security_check', { form: { _charset_: 'utf-8', j_username: process.env.AEM_AUTHOR_USERNAME || 'admin', j_password: process.env.AEM_AUTHOR_PASSWORD || 'admin', j_validate: 'true' } }); if (!response.ok()) throw new Error(`AEM author login failed (HTTP ${response.status()}) at ${baseURL}.`); await ctx.storageState({ path: state }); await ctx.dispose(); };
